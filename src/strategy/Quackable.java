/**
 * 
 */
package strategy;

/**
 * All objects implementing this interface must implement the method to quack,
 * but every object can have a different behavior of the same method
 * 
 * @author Leonardo
 */
public interface Quackable {
	public String quack();
}
